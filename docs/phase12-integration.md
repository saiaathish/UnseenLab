# Phase 12 — Final Integration Record

Status: UNMERGED — both PRs remain DRAFT. Record generated by final-integration-engineer, not committed.

## Reviewed range

- Branch: `fix/generative-trust-controls`
- Final SHA: `f7ea5b26fd7ca4972bf852635dda784b085a3c6f`
- Stacked on PR #9 head: `0d889572f0c8d5c47545f6e6b041df3ec52460f7`
- Commits (`0d88957..HEAD`, 5): decision table, materialization, docs/evidence, holdout v3 freeze, v3 results
- Scope: contained — only `src/demonstrations` (trust/controls/prompt/schema/pipeline) + `tests/demonstrations` + `docs/` + `scripts/holdout-v3-*`. 25 files, +7934/−8.
- `src/demonstrations/validation/sanitize.ts`: empty diff (no repair expansion).
- Pre-existing holdout files (`holdout-2026-08-05.md`, `holdout-2026-08-06-v2.md`, `holdout-results-2026-08-05.json`, `holdout-v2-results-2026-08-06.json`): untouched.

## Final gate (exact outputs, run on f7ea5b2 in worktree)

| Check | Result |
| --- | --- |
| `npm run lint` | exit 0 |
| `npm run typecheck` | exit 0 |
| `npm test` | 1224 passed, 1 failed (1225 total, 72 files: 71 passed, 1 failed) |
| `npm run build` | exit 0 |
| `node scripts/bundle-secret-scan.mjs` | OK: no secret material in client bundles; server env reads intact (FIREBASE_SERVICE_ACCOUNT 3/3, MONGODB_URI 9/9 PRESENT server-side) |
| `git diff --check 0d88957..HEAD` | clean |
| `git status --short` | clean |

Intended contract failure (1/1225): `tests/demonstrations/backend/database-security-contract.test.ts` — app DB user holds `atlasAdmin` on live cluster; blocked-external, documented in `docs/phase12-persistence.md` / `docs/phase12-security.md`.

## CI + preview

- CI runs on stacked head: 1 run, workflow `CI`, event `pull_request`, headSha `f7ea5b2…`, status `completed`, conclusion `success`. No deploy workflow for this branch.
- PR #10 (`fix/generative-trust-controls` → `feature/generative-demonstration-engine`): OPEN, draft, MERGEABLE.
- PR #9: unchanged — draft, head `0d889572f0c8d5c47545f6e6b041df3ec52460f7`.
- Production: `origin/main` = `75fbd73b` (unchanged); no feature-branch production deployment.

## Flag gate

- `src/demonstrations/feature-flag.ts`: `GENERATIVE_DEMOS_ENABLED = (process.env[ENV_KEY] ?? "0") === "1"` — default `"0"`.
- Diff for `feature-flag.ts` in `0d88957..HEAD`: empty. Stacked branch changes nothing about the flag.

## Holdout v3 results (artifact `docs/holdout-v3-results-2026-08-06.json`)

- trust: 0.9286 ≥ 0.90 → PASS
- controlRelevance: 0.875 ≥ 0.85 → PASS
- renderable: 0.9643 < 0.98 → FAIL
- useful: 0.9286 ≥ 0.85 PASS · unsafe: 1.0 = 1.0 PASS · accessible: 1.0 = 1.0 PASS · materialization: 1.0

## Verdict

- Phase 1: BLOCKED (HUMAN_ACTION_REQUIRED) — least-privilege DB user remediation is external.
- Phase 2: FAIL — renderable 96.4% below 98% gate despite trust and control-relevance passing.
- Both PRs stay DRAFT. No merge, no push, no deployment.

---

# Round 2 final (final-integration-engineer, 2026-08-06)

Status: UNMERGED — both PRs remain DRAFT. Record generated by final-integration-engineer, not committed.

## Reviewed range

- Final SHA: `12518d3f4ac912e87e7d428974de1e8addd3c61f` (branch `fix/generative-trust-controls`)
- Commits (`0d88957..HEAD`, 11 — brief expected 9, actual rev-list count is 11):
  `adb6d96` decision-table test, `40238bf` materialize controls, `60cb2be` Phase 1-2 closure evidence docs, `311d1b2` v3 holdout freeze, `f7ea5b2` v3 results, `42ac529` v3 diagnosis correction, `c3870ef` integration gate docs, `d040446` runtime trust wiring + focus-key ranking, `01d2d8d` verification-claims correction, `f0a1e6d` v4 holdout freeze, `12518d3` v4 results (no rules changed)
- Scope: contained — `src/demonstrations` (trust/controls/prompt/schema/pipeline/route) + `tests/demonstrations` + `docs/` + new `scripts/holdout-v3/v4-*` + `docs/holdout-2026-08-06-v{3,4}`. 38 files, +15057/−23.
- `src/demonstrations/validation/sanitize.ts`: empty diff (no repair expansion).
- Old holdout files (`scripts/holdout-manifest-2026-08-0{5,6}.mjs`, `docs/holdout-2026-08-05.md`, `docs/holdout-2026-08-06-v2.md`): untouched in range.

## Final gate (exact outputs, run on 12518d3 in worktree)

Interpreter: real node v24.17.0 (`/Users/saiaathishkarthik/.local/lib/node-v24.17.0-darwin-arm64/bin`) — PATH `node` is a bun shim; vitest invoked as `node ./node_modules/vitest/vitest.mjs run`.

| Check | Result |
| --- | --- |
| `npm run lint` | **FAIL exit 1** — 2 errors in `scripts/holdout-v4-runner-2026-08-06.mjs`: 100:25 `pathToFileURL` unused, 578:10 `specRowPassesValidation` unused (introduced by `f0a1e6d`) |
| `npm run typecheck` | exit 0 |
| `npm test` | 1291 passed, 1 failed (1292 total, 74 files: 73 passed, 1 failed) |
| `node ./node_modules/next/dist/bin/next build` | exit 0 |
| `git diff --check 0d88957..HEAD` | clean |
| `git status --short` | clean |

Intended contract failure (1/1292, unchanged from base, no diff in range): `tests/demonstrations/backend/database-security-contract.test.ts` — live local Atlas app user still holds `atlasAdmin` on `admin` db; blocked-external, human Atlas console remediation (Phase 1). CI unit tests passed at `01d2d8d` (run 31073320581) with the same test file.

## CI + PRs

- CI on final head 12518d3: run 31079970187, conclusion **failure** — job "Lint, typecheck & unit tests" failed at **Lint** (same 2 unused-var errors under Node 22; typecheck + unit tests SKIPPED by fail-fast). E2E (Playwright), Production build, Secrets scan: success.
- PR #10 (`fix/generative-trust-controls` → `feature/generative-demonstration-engine`): OPEN, **draft**, MERGEABLE, head `12518d3f4ac912e87e7d428974de1e8addd3c61f`.
- PR #9: unchanged — OPEN, draft, head `0d889572f0c8d5c47545f6e6b041df3ec52460f7`.
- Production: `origin/main` = `75fbd73b176a3b790e328052d99370bc94f23636` (untouched); no feature-branch production deployment (draft PRs).

## Holdout v4 results (artifact `docs/holdout-v4-results-2026-08-06.json`, single run, base localhost:3200, 43 prompts, started 2026-08-06T07:07:04Z)

- TRUST classification 96.6% (28/29) ≥ 90% → PASS (was 92.9% v3)
- CONTROL RELEVANCE 65.0% (13/20) < 85% → **FAIL** — gold-authoring over-declaration vs oneVariableMode materialization (every miss row's ranked keys contain all expected keys; materialized single control = ranked first key; coverage 1/3 on 3-key golds, 1/2 on 2-key golds). Materialization contract 20/20, ranking tiers verified.
- VALID-SPEC 100%, RENDERABLE-AFTER-VALIDATION 100%, ACCESSIBLE 100%, AVAILABILITY 100% (no threshold, reasons {}) — v3 renderable failure class eliminated
- USEFUL 96.6% (28/29) ≥ 85% PASS · UNSAFE 100% (3/3) · ESCALATION 0 · latency p50 12081 ms / p95 21171 ms · gate: FAIL

## Verdict (Round 2 final)

- Phase 1: BLOCKED (HUMAN_ACTION_REQUIRED) — least-privilege Atlas user remediation is a human console step; locally evidenced by the still-failing security-contract test (atlasAdmin held).
- Phase 2: FAIL — v4 control relevance 65% below the 85% gate; diagnosed as a gold-authoring contract violation (frozen red-team condition), not a product defect; trust 96.6% PASS and valid-spec/renderable/availability 100%.
- Additional gate finding: lint is RED on the final head (2 unused vars in the v4 runner) — identical on local Node 24 and CI Node 22; CI unit tests were skipped on the final head (fail-fast).
- Both PRs stay DRAFT. No merge, no push, no deployment.
